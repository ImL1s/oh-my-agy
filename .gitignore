# deps / build
node_modules/
dist/

# runtime state (do not ship)
.omx/
.omc/
.agy/
.agy_sandbox_*/
# project-local slash skill symlinks created by `oma setup` (sources live in skills/)
.claude/
.grok/
*.log
.DS_Store
coverage/
*.tsbuildinfo

# secrets / credentials
.env
.env.*
*.pem
*.key
credentials.json
**/secrets/**

# workspace-local agent research dumps under .agents (keep hooks.json only)
.agents/**
!.agents/hooks.json

# local research / scratch (not shipped)
ORIGINAL_REQUEST.md
research_report.md
test_todo.json

# Octopus session handoff scratch（工具暫存，不入版控）
.octo-continue.md
