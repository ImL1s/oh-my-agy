{
  "name": "oh-my-agy",
  "version": "1.0.0",
  "description": "Google Antigravity CLI (agy) Wrapper & Continuation Enforcer",
  "main": "bin/oma.ts",
  "bin": {
    "oma": "dist/bin/oma.js",
    "omy": "dist/bin/oma.js"
  },
  "files": [
    "dist",
    "plugin.json",
    "hooks.json",
    "skills",
    "rules",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "build": "tsc",
    "prepack": "npm run build",
    "test:unit": "jest --config jest.unit.config.js --runInBand",
    "test:e2e": "jest --config jest.config.js --runInBand",
    "test:package": "jest --config jest.unit.config.js --runInBand tests/package"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.24",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
